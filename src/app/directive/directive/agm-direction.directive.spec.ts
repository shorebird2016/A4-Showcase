import { AgmDirectionDirective } from './agm-direction.directive';

describe('AgmDirectionDirective', () => {
  it('should create an instance', () => {
    const directive = new AgmDirectionDirective();
    expect(directive).toBeTruthy();
  });
});
