import { SbAutogrowDirective } from './sb-autogrow.directive';

describe('SbAutogrowDirective', () => {
  it('should create an instance', () => {
    const directive = new SbAutogrowDirective();
    expect(directive).toBeTruthy();
  });
});
