import { CollapsableDirective } from './collapsable.directive';

describe('CollapsableDirective', () => {
  it('should create an instance', () => {
    const directive = new CollapsableDirective();
    expect(directive).toBeTruthy();
  });
});
